"""Hand-written stubs for python-frontmatter.

Covers the narrow surface we actually use: ``load``, ``loads``, ``dumps``,
and ``Post.metadata``/``Post.content``. Expand as new call sites appear.
"""

from collections.abc import Iterable
from os import PathLike
from typing import IO

__all__ = ["Post", "dumps", "load", "loads"]

class Post:
    metadata: dict[str, object]
    content: str

    def __init__(self, content: str, **metadata: object) -> None: ...
    def __contains__(self, item: object) -> bool: ...
    def __getitem__(self, name: str) -> object: ...
    def __setitem__(self, name: str, value: object) -> None: ...
    def __delitem__(self, name: str) -> None: ...
    def __bytes__(self) -> bytes: ...
    def __str__(self) -> str: ...
    def get(self, key: str, default: object = ...) -> object: ...
    def keys(self) -> Iterable[str]: ...
    def values(self) -> Iterable[object]: ...
    def to_dict(self) -> dict[str, object]: ...

def load(
    fd: str | PathLike[str] | IO[str] | IO[bytes],
    encoding: str = ...,
    **defaults: object,
) -> Post: ...
def loads(text: str, encoding: str = ..., **defaults: object) -> Post: ...
def dumps(post: Post, **kwargs: object) -> str: ...
